logging:
  level:
    com.jkys.consult.infrastructure.db.mapper: info

debug: false

server:
  port: 9241

spring:
#  profiles:
#    active:
#      include: mybatisplus

  freemarker:
    template-loader-path: classpath:/templates/ #模板加载路径 按需配置
    suffix: .ftl  #后缀名
    content-type: text/html
    enabled: true
    cache: false #缓存配置
    charset: UTF-8 #编码格式
  main:
    banner-mode: "off"

